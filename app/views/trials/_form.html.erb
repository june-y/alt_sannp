<%= form_with(model: trial, local: true) do |form| %>
  <% if trial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trial.errors.count, "error") %> prohibited this trial from being saved:</h2>

      <ul>
      <% trial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
